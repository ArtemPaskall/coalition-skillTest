{
  "name": "coalition-skillTest",
  "version": "0.1.0",
  "description": " Coalition Technologies Frontend Skill Test by Artem Paskal.",
  "scripts": {
    "clean": "rimraf dist",
    "start": "npm run clean && parcel serve ./src/index.html",
    "build": "npm run clean && parcel build ./src/index.html --public-url ./",
    "deploy": "npm run build && gh-pages -d dist"
 },
  "author": "Artem Paskal",
  "license": "MIT",
  "homepage": "https://artempaskall.github.io/coalition-skillTest/",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.0.0",
    "gh-pages": "^6.0.0",
    "parcel": "^2.0.0",
    "posthtml-include": "^1.7.2",
    "rimraf": "^3.0.2",
    "stylelint": "^15.10.3",
    "stylelint-scss": "^5.2.1"
  },
  "dependencies": {
    "prismjs": "^1.25.0"
  }
}